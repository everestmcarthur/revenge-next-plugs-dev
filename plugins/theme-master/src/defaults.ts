import type { ThemeMasterStorage } from './types'

export const DEFAULTS: ThemeMasterStorage = {
	themes: {},
	fonts: {},
	selectedThemeId: null,
	selectedFontName: null,
	overrideThemeType: 'auto',
	showChatBackground: true,
}
